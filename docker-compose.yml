version: "3"

services:
  web:
    build: .
    container_name: localauth0_web
    volumes:
      - .:$PWD:cached
    ports:
      - "3000:3000"
    working_dir: $PWD
    environment:
      CARGO_HOME: /home/app/.cargo
    networks:
      - default
